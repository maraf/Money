@if (Selected == ExpenseCreateDialogType.Standard)
{
    <OutcomeCreate @ref="OutcomeCreateRef" />
}
else if (Selected == ExpenseCreateDialogType.Wizard)
{
    <ExpenseCreate @ref="ExpenseCreateRef" />
}