@inherits ModalDialog

<Modal @ref="Modal" Title="Change when the expense occured">
    <ChildContent>
        <div class="mb-3">
            <label class="form-label" for="expense-when">When:</label>
            <CalendarPicker Id="expense-when" @bind-Value="When" />
        </div>
    </ChildContent>
    <Buttons>
        <button class="btn btn-primary" @onclick="@OnSaveClick">Save</button>
    </Buttons>
</Modal>
