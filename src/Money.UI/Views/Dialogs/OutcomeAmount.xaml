<ContentDialog
    x:Class="Money.Views.Dialogs.OutcomeAmount"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d" PrimaryButtonText="Save" SecondaryButtonText="Cancel" Title="Amount">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="120" />
        </Grid.ColumnDefinitions>
        
        <TextBox x:Name="tbxAmount" InputScope="Number" FontSize="40" TextAlignment="Right" GotFocus="tbxAmount_GotFocus" KeyDown="tbxAmount_KeyDown" />

        <ComboBox x:Name="cbxCurrency" DisplayMemberPath="Symbol" FontSize="40" HorizontalAlignment="Stretch" Grid.Column="1" />
    </Grid>
</ContentDialog>
